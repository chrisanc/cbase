package com.backend.ai.adapters;

import com.backend.ai.ports.Tokenizer;

public class TokenizerImpl implements Tokenizer {
    @Override
    public float[] tokenize(String line) {
        return new float[0];
    }
}
